[pytest]
pythonpath = src
testpaths = tests
norecursedirs = .venv .hypothesis
addopts = --cov=src --cov-report=term-missing
markers =
    integration: tests that hit live external services
    fuzz: property-based fuzzing tests
    live: tests requiring real API keys or network access
